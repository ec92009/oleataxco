# Codex Daily Review — 2026-05-09

## Architecture
- Clear scope (single draft homepage + `preview.html` variants) keeps the repo focused; keep shared assets centralized under `assets/`.
- The “3 directions” preview is valuable—consider ensuring each option is implemented via shared tokens to avoid triple-maintenance.

## UI
- The A/B/C preview framing is excellent for stakeholder feedback; keep each direction visually distinct enough that a choice is obvious.
- Dark mode toggle is a must-have; ensure transitions are smooth and no “flash” occurs on load.

## UX
- The scheduling placeholder is the right kind of fake; add microcopy that sets expectations (“no booking yet”) to avoid confusion.
- Make the “Book a Call” section work well on mobile (tap targets + scroll positioning).

## Misc
- `content-workbook.md` is a good companion—keep it in sync with whichever direction wins to avoid stale content.
